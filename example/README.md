# react-raptorize (Example App) 🦖

Installs the package locally for development purposes. 

1. Run `npm run build` from the repository root directory.
2. Run `npm install` from this directory (/example).
3. Run the app with `npm run dev` to see it in action.

⚠️ NOTE: This example app, while working, needs to be updated once the package build is improved and migrated to a real package vs just a JS file. We'll get there, but let's do it progressively. For example, the way we're packaging react-raptorize `index.js` via `tsconfig.node.js` and `vite.config.ts` needs undone, or at least moved to the root.